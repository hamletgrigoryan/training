#include <iostream>
#include "circle.h"

/*
int main()
{
	double x, y, radius;
	std::cout << "Input coordinates of circle's center" << "\nx = ";
	std::cin >> x;
	std::cout <<"\ny = ";
	std::cin >> y;
	Point center(x, y);
	std::cout << "\nInput the radius" << "\nradius = ";
	std::cin >> radius;
	Circle circle(center, radius);
	
	std::cout << "Input a point to check if it is in the circle " << "\nx =";
	std::cin >> x;
	std::cout <<"\ny = ";
	std::cin >> y;
	std::cout << "point = " << "(" << x << "," << y << ")" << std::endl;
	Point p1(x,y);
	if (isInnerPoint(circle, p1))
	{
		std::cout << "the point is in the circle...";
	}
	else {
		std::cout << "The point is out of circle!";
	}
	double a;std::cin >> a;
	return 0;
}
*/